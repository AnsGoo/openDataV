import customScript, { makeFunction } from './custom'
import ststemScript from './system'
import type { BaseScript, CallbackType, ScriptOption, Slotter } from './type'

export type { BaseScript, CallbackType, ScriptOption, Slotter }

export { customScript, makeFunction, ststemScript }
